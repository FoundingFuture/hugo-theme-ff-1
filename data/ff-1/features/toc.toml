name = "toc"
slot = "page.before-body"
weight = 10
default = true
level = "toggle"
kinds = ["page"]
partial = "features/toc.html"
css = "css/features/toc.css"
i18n = ["contents"]

[skeleton]
  # It folds, so the summary's parts are marked as well. The list it
  # holds sits inside the nav Fragments.ToHTML writes, and chrome is
  # not part of a page's shape.
  text = ["details.toc", "span.nm", "span.n", "span.chev"]
