name = "code-copy"
summary = "A button on a code block that puts its text on the clipboard."
slot = "body.end"
weight = 20
default = false
level = "toggle"
kinds = ["page"]
partial = "features/code-copy.html"
css = "css/features/code-copy.css"
i18n = ["copy"]

[skeleton]
  # The button is added by a script, so the built page carries none.
  text = []
